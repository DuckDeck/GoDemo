CREATE TABLE `TuYi`.`beautiful` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(100) NOT NULL,
  `url` VARCHAR(100) NOT NULL,
  `cat` VARCHAR(45) NOT NULL,
  `imgs` NVARCHAR(1500) NOT NULL,
  PRIMARY KEY (`id`));
